# ImongCareer Backend

This backend runs in two modes:

- Local: `npm run dev` (Express server on `PORT`)
- Vercel serverless: exported via `backend/api/index.js`

For single-project Vercel deployment, see root `vercel.json`.
